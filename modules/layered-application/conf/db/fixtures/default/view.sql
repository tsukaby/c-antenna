#!Ups

DROP VIEW IF EXISTS SITE_SUMMARY;
CREATE VIEW SITE_SUMMARY AS
  SELECT
    S.ID,
    S.NAME,
    S.URL,
    S.THUMBNAIL,
    COUNT(1) AS ARTICLE_COUNT,
    S.CLICK_COUNT,
    S.HATEBU_COUNT
  FROM
    ARTICLE A, SITE S
  WHERE
    S.ID = A.SITE_ID
  GROUP BY
    A.SITE_ID
