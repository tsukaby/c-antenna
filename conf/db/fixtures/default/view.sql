#!Ups

DROP VIEW IF EXISTS SITE_SUMMARY;
CREATE VIEW SITE_SUMMARY AS
  SELECT
    S.ID,
    S.NAME,
    S.URL,
    COUNT(1) AS COUNT
  FROM
    ARTICLE A, SITE S
  WHERE
    S.ID = A.SITE_ID
  GROUP BY
    A.SITE_ID
  ORDER BY
    COUNT DESC;


